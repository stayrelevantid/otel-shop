apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: otel-shop
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - name: http
      port: 18080
      targetPort: http
      nodePort: 18080