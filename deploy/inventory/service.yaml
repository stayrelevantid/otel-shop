apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: otel-shop
spec:
  type: NodePort
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 18081
      targetPort: http
      nodePort: 18081